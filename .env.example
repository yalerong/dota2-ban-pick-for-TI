# OpenDota API key (optional; free tier works without it: 60 req/min, 3000 req/day (measured; the daily response header is authoritative))
OPENDOTA_API_KEY=
# Data directory (default: ./data)
BP_DATA_DIR=
