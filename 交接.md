# 交接：dota2-ban-pick-for-TI（2026-08-26）

仓库 https://github.com/yalerong/dota2-ban-pick-for-TI ，本地就是这个文件夹。main 已含 Phase 1 + Phase 2（PR#1、PR#2 已合并）。

## 2026-08-27 增量

- PR#3（针对 Ban=超出 meta 基线）Codex P2 已修：`meta_ban_rates(fr)` 独立算全帧 ban 率，`signature_scores` 不再经过该队 ban 表；待合并。
- PR#4：`scripts/backfill.py` 改成按墙钟分块睡——笔记本休眠期间 `time.sleep` 不走表，08-27 醒来晚了近 2 小时、当日额度没动；待合并。
- 回填已用新脚本重启（PID 见 `data/backfill.pid`，工作区停在分支 `backfill-wallclock-sleep`）；按 3000/天、余 ~4680 场，约还要 2 天。
- 主目录本身是 git 仓；PR#3/#4 合并后 `git checkout main && git pull` 再续。

## 现在停在哪

- **7.41 版本回填只跑了一半**：索引 7200 场，详情已拉 ~2800 场（干净样本 2577），还剩 ~4670 场。
- 后台进程 `scripts/backfill.py`（PID 见 `data/backfill.pid`）在睡等 OpenDota 次日额度，机器睡眠它就暂停，醒了继续；杀掉也没事，可断点续跑。
- Phase 2 的方法论已经在**半份数据**上出了一版基线（`docs/baseline.md`），**等全量数据来验证**——这是下一步，也是唯一必须做的事。

## 想起来时怎么续（按顺序，每步都可重复执行）

```powershell
cd "C:\Users\z1628\Desktop\dota bp"
$env:PYTHONPATH="src"; $env:PYTHONIOENCODING="utf-8"

# 0. 看进度（pending 是还没拉的场次；budget_left_today 是今天剩余额度，日限 3000）
python -m bp status

# 1. 续回填：要么直接跑驱动脚本（会自己跨天睡等额度，直到 pending=0）
python scripts\backfill.py --since 2026-03-24
#    要么手动一天一次（额度用完自动停，第二天再跑）
python -m bp update --since 2026-03-24

# 2. pending=0 后：按 docs/baseline.md 冻结的协议验证
python -m bp export --as-of 2026-08-01            # 重导同一天的快照（训练数据变多，as_of 不变）
#    记下输出里的 path，然后同一批测试场跑开/关两版：
python -m bp blindtest --snapshot data\snapshots\snapshot_20260801_<新hash>.sqlite --patch 7.41 --max 120 --out docs\baseline-run2-on.md
python -m bp blindtest --snapshot data\snapshots\snapshot_20260801_<新hash>.sqlite --patch 7.41 --max 120 --no-context --out docs\baseline-run2-off.md
#    把两张表追加到 docs/baseline.md 的 "Run 2" 节，和 Run 1 对比。权重不要按结果调。
```

验证要回答的两个问题：训练场次从 ~2200 涨到 ~6000 后，Top-3 命中涨没涨；阵容上下文项（克制/协同/角色缺口）在大样本下是否仍然对 Ban 有帮助、对 Pick 不再拖后腿。

## 验证之后的分岔

- 上下文项站得住 → 做后期 pick 短板（末手 pick 现在还输元游戏频率基线，缺"针对当前阵容"的更强项）。
- 站不住 → `config/scoring.yaml` 里 `context.*` 权重归零，产品只保留画像/习惯层，同样能出报告。
- 独立可做：Phase 3 DraftBook（教练 Markdown 规则注入，PLAN.md 有设计）；P2-11 Streamlit 看板（可砍）。

## H5 手机页（测试用，本地）

```powershell
python -m bp h5 --matchup "Team Spirit|Team Liquid" [--matchup "A|B" ...]   # 生成 h5\index.html（单文件、图标已内嵌、离线可用）
python -m http.server 8000 -d h5                                          # 手机连同一 Wi-Fi 打开 http://<电脑IP>:8000/
```

两个 tab：**天梯**（选位置 + 点英雄加到我方/敌方/禁用 → 克制/协同/补位/位置热度推荐）；**比赛 BP**（赛前报告折叠视图 + Draft Board 简化版，`#pro` / `#board` 可直达）。页内评分是 recommend.py 的**简化镜像**（无习惯/回应项），权威结果仍以 `bp report` / `bp draft` 为准。英雄图标首次从 Steam CDN 下载缓存到 `data/raw/hero_icons/`。

## 日常用法

```powershell
python -m bp teams --q spirit                                  # 找战队
python -m bp report --us "Team Spirit" --them "Team Liquid"    # 赛前报告 -> reports\
python -m bp draft  --us "Team Spirit" --them "Team Liquid" --first them   # 交互 Draft Board
```

## 已知坑（都记在 memory 里，这里留一份）

- OpenDota 日限额以响应头为准（实测 3000），客户端 45/min；`get_cached` 曾漏拼 match_id 导致全 404，已修有测试。
- Draft 格式是从数据推断的（`bp formats`），换版本不用改代码；异常场次自动剔除。
- 位置用 lane_role+GPM 推断，2/3 号位偶尔混、游走 4 号位可能错。
- 每场都缺 `draft_timings`（OpenDota 未解析 replay），是软标记不是问题。
- 这个项目上别用 `/code-review`：主目录 C:\Users\z1628 本身是 git 仓，它会审错目标。
- Windows 控制台 GBK 会被选手名炸掉，CLI 已强制 utf-8；写脚本时加 `PYTHONIOENCODING=utf-8`。
- 电源"平衡"计划插电 5 分钟睡眠，长跑要么 `powercfg /change standby-timeout-ac 0`，要么接受暂停。

## 数据文件（不进 git）

`data/raw/matches/*.json` 原始详情（每场 ~285KB，全量约 2GB）；`data/db/bp.sqlite` 规范化库；`data/snapshots/` 冻结快照；`data/backfill.log` 回填日志。
