# 交接：dota2-ban-pick-for-TI（2026-08-26）

仓库 https://github.com/yalerong/dota2-ban-pick-for-TI ，本地就是这个文件夹。main 已含 Phase 1 + Phase 2（PR#1、PR#2 已合并）。

## 2026-08-27 增量

- PR#3、PR#4、PR#6 已合并；PR#6 清掉了旧 review 中 16 条仍成立的问题，包括 cutoff 泄漏、混合 patch
  blind test、证据链和 wheel 配置。
- 已下载数据用合并后的代码完整重建：7367 场、6949 clean、有效时间范围内无 pending；逐场选手名已补齐。
- 采用**增量 checkpoint**，不再要求等全部回填完成才验证。下载进程仍由 `data/backfill.pid` 记录，当前等待
  下一次 UTC 日额度重置后继续同步新比赛。
- Run 2 已完成：固定测试集 `6d348cc4215c4225`，snapshot `98ead296cc1f3808`，结果见 `docs/baseline.md`。
- Run 3 已完成：snapshot `58e7a04b5d620dcd`；新增独立开发集 `c8b39c666b2141bc`。bans-only context
  未明确胜过 all-actions，因此只保留实验开关，没有修改生产默认。
- 天梯 H5 已改成一键演示入口；职业训练页支持离线打包多支战队、分别选择双方、一键交换和任意组合 Draft Board。

## 现在停在哪

- **Run 2 checkpoint 已完成**：训练 snapshot 内 clean 从约 2200 增到 4991，但整体 Top-3 仍为 21.7%；
  Ban 提升到 23.4%，Pick 降到 19.4%。
- Context 的方向在更多数据下重复：对 Ban +1.0pt、对 Pick -0.8pt；末阶段 Pick 9.2%，仍低于 meta 9.6%。
- 冻结的 120 场验收集已写入 `docs/baseline-test-matches.json`，后续增量 checkpoint 和模型晋级必须复用，
  不能再按 snapshot 动态选择测试场。
- 下一步是用**独立开发集**验证 action-specific context（Ban/Pick 分开处理），不能直接按冻结测试集调权重。

## 想起来时怎么续（按顺序，每步都可重复执行）

```powershell
cd "C:\Users\z1628\Desktop\dota bp"
$env:PYTHONPATH="src"; $env:PYTHONIOENCODING="utf-8"

# 0. 看进度（pending 是还没拉的场次；budget_left_today 是今天剩余额度，日限 3000）
python -m bp status

# 1. 需要下一批增量数据时续回填；达到预定 checkpoint 后可安全停止
python scripts\backfill.py --since 2026-03-24
#    或手动跑一轮
python -m bp update --since 2026-03-24

# 2. 每个 checkpoint 按 docs/baseline.md 冻结的协议验证；不要求 pending=0
python -m bp normalize --rebuild
python -m bp formats
python -m bp check
python -m bp export --as-of 2026-08-01            # 重导同一天的快照（训练数据变多，as_of 不变）
#    记下输出 path；必须传固定 match IDs，同一批场次跑 ON/OFF：
python -m bp blindtest --snapshot data\snapshots\snapshot_20260801_<新hash>.sqlite --patch 7.41 --max 120 --test-matches docs\baseline-test-matches.json --out docs\baseline-runN-on.md
python -m bp blindtest --snapshot data\snapshots\snapshot_20260801_<新hash>.sqlite --patch 7.41 --max 120 --test-matches docs\baseline-test-matches.json --no-context --out docs\baseline-runN-off.md
#    追加到 docs/baseline.md，记录 snapshot/test-set hash；权重不要按验收集结果调。
```

Run 2 已回答：训练数据翻倍没有提高整体 Top-3；上下文仍帮助 Ban、拖累 Pick。下一项实验必须在独立开发集上
验证 Ban/Pick 分开的 context 处理，再回到冻结验收集做一次晋级判断。

## 验证之后的分岔

- Context 对 Ban 站得住、对 Pick 站不住 → 在独立开发集测试 action-specific context；不要直接改全局权重。
- 末阶段 Pick 仍是首要短板，需要加入比当前 pairwise 平均项更针对当前阵容的证据，但必须通过独立开发集选择。
- 独立可做：Phase 3 DraftBook（教练 Markdown 规则注入，PLAN.md 有设计）；P2-11 Streamlit 看板（可砍）。

## H5 手机页（测试用，本地）

```powershell
python -m bp h5 --matchup "Team Spirit|Team Liquid" --team OG --team 9247354 # --team 可重复，加入离线战队选择器
python -m http.server 8000 -d h5                                          # 手机连同一 Wi-Fi 打开 http://<电脑IP>:8000/
```

两个 tab：**天梯副驾驶**（拍照/导入主入口、明确标注的示例试算、一个主推荐 + 三个备选、折叠纠错）；**职业训练**（双方战队选择、交换、对阵摘要 + Draft Board，`#pro` / `#board` 可直达）。真实截图识别尚未接入，必须先收集实际客户端截图校准；演示按钮不会伪装成真实识别。页内评分是 recommend.py 的**简化镜像**（无习惯/回应项），权威结果仍以 `bp report` / `bp draft` 为准。英雄图标首次从 Steam CDN 下载缓存到 `data/raw/hero_icons/`；中文名/俗称在 `config/hero_zh.yaml`（输入联想用，加别名改这里）。

## 日常用法

```powershell
python -m bp teams --q spirit                                  # 找战队
python -m bp report --us "Team Spirit" --them "Team Liquid"    # 赛前报告 -> reports\
python -m bp draft  --us "Team Spirit" --them "Team Liquid" --first them   # 交互 Draft Board
```

## 已知坑（都记在 memory 里，这里留一份）

- OpenDota 日限额以响应头为准（实测 3000），客户端 45/min；`get_cached` 曾漏拼 match_id 导致全 404，已修有测试。
- Draft 格式是从数据推断的（`bp formats`），换版本不用改代码；异常场次自动剔除。
- 位置用 lane_role+GPM 推断，2/3 号位偶尔混、游走 4 号位可能错。
- 每场都缺 `draft_timings`（OpenDota 未解析 replay），是软标记不是问题。
- 这个项目上别用 `/code-review`：主目录 C:\Users\z1628 本身是 git 仓，它会审错目标。
- Windows 控制台 GBK 会被选手名炸掉，CLI 已强制 utf-8；写脚本时加 `PYTHONIOENCODING=utf-8`。
- 电源"平衡"计划插电 5 分钟睡眠，长跑要么 `powercfg /change standby-timeout-ac 0`，要么接受暂停。

## 数据文件（不进 git）

`data/raw/matches/*.json` 原始详情（每场 ~285KB，全量约 2GB）；`data/db/bp.sqlite` 规范化库；`data/snapshots/` 冻结快照；`data/backfill.log` 回填日志。
